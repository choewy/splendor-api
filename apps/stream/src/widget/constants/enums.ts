export enum WidgetSubChannel {
  Exception = 'exception',
  Setting = 'setting',
  Play = 'play',
  Clear = 'clear',
}

export enum WidgetPubChannel {
  Setting = 'setting',
  PlayComplete = 'play',
}
